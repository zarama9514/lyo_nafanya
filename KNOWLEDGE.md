# Лиофилизация: контекст задачи и накопленные знания

Сводный документ по проекту моделирования первичной сушки при лиофилизации.
Перенос всех знаний, выводов и решений, полученных в ходе работы.

- **Рабочая директория:** `/Users/zarama9514/python_projects/boris`
- **Git:** https://github.com/zarama9514/lyo_nafanya
- **Окружение:** `uv` (numpy, scipy, matplotlib, pypdf)

---

## 1. Контекст и постановка задачи

**Исходный запрос.** Изучить предложенную литературу о выборе начальных условий и
методах лиофилизационной сушки и предложить, как **физически обоснованно** усилить
модель из статьи *Tang & Pikal, «Design of Freeze-Drying Processes for
Pharmaceuticals: Practical Advice»* для более грамотного и эффективного режима.
**Эффективность = минимум времени получения образца с минимальными дефектами
(или без коллапса).**

Два явных под-вопроса:
1. Реальный продукт может коллапсировать, поведение неидеально, есть градиенты
   температуры и концентрации — можно ли это физически обоснованно учесть?
2. Реальный процесс заморозки — как его учесть?

Ключевое ограничение, добавленное по ходу: **усиливать модель только тем, что НЕ
требует новых экспериментальных величин** (нельзя: измеренный `Rp`/MTM, `Tc`/FDM,
изотермы сорбции, измеренный размер кристаллов, температура нуклеации). Можно:
первопринципная физика, табличные константы, теоретические замыкания и уже
доступные входы (состав/концентрация, геометрия флакона и шкафа, `Tg'`, `Tim`,
`Rs`, `Kv`).

**Литература (PDF в рабочей папке, в git не залиты — копирайт):**
- `tang2004.pdf` — Tang & Pikal, Pharm. Res. 21(2):191–200 (2004). Базовая модель.
- `1-s2.0-S0255270102000806-main.pdf` — Ronzi et al., Chem. Eng. Process. 42 (2003)
  751–757. Оптимизация цикла Factor VIII/IX. Используется для валидации.
- `1-s2.0-S1773224724007585-main.pdf` — Kumar et al., J. Drug Deliv. Sci. Technol.
  100 (2024) 106089. Обзор: стрессы заморозки, QbD, scale-up.

---

## 2. Базовая модель (Tang & Pikal, 2004)

Квазистационарная, сосредоточенная (0-D на флакон):
- `dm/dt = (Pice − Pc)/(Rp + Rs)` — массоперенос (ур.1)
- `Pc = 0.29·10^(0.019·Tp)` — «оптимальное» давление (ур.2)
- `dQ/dt = 3600·Av·Kv·(Ts − Tb)` — теплоперенос (ур.3)
- `dQ/dt = ΔHs·dm/dt` — энергобаланс (ур.4)
- `Ts = Tp + (1/Av)(dQ/dt)(1/Kv + l_ice/k1)` — нужная T полки (ур.5)
- `Kv = KC + 3.32·10⁻³·P/(1 + KD·P)` — теплопередача флакон↔полка (ур.6)

Правила: целевая `Tp = Tc − запас` (2–5 °C); +1 °C к Tp ускоряет сушку ~на 13 %.

**Важно про `Rp` и концентрацию в Tang & Pikal:** ФОРМУЛЫ `Rp(c)` НЕТ. Есть только
качественное «выше концентрация → выше Rp» и два репера: `<1 %` солидов → `R̂p≈1`,
`>10 %` → `R̂p≈5` Torr·см²·ч/г (на ~1 см), плюс категории на рисунках. Число 5 и
форма `R̂p = Rp·Ap` — со ссылкой [28] = Pikal, Shah, Senior, Lang (1983).

---

## 3. Наша улучшенная модель (`lyo_model.py`)

### 3.1 Что добавлено сверх базовой
1. **Подвижный фронт сублимации (задача Стефана)** — 1-D, явный градиент `Tb−Tp`,
   меняющийся во времени, вместо одной алгебраической поправки.
2. **`Rp(H)`** — растёт с толщиной сухого слоя; параметры связаны с заморозкой.
3. **Связь «заморозка → Rp»** — переохлаждение/режим → размер кристаллов → поры.
4. **Динамика во времени** — `Tp(t)` дрейфует (старт теплолимит, финал массолимит).
5. **Критерий коллапса** — `Co = η(Tc)/η(Tp)` (WLF), привязка к измеряемой `Tc`.

### 3.2 Ключевые уравнения (численная схема)
На каждом шаге решается квазистационарная система относительно `(Tp, Tb, Js)`:
```
Js          = (Pice(Tp) − Pc)/Rp(H)
Js·ΔHsub    = Kv·(Av/Ap)·(Ts − Tb)        [тепло: полка → дно]
Js·ΔHsub    = (k_ice/(L0−H))·(Tb − Tp)    [тепло: дно → фронт через лёд]
dH/dt       = Js/ρ_cake
```
Решается бисекцией по `Tp` в `[Tp_floor, Ts]`, где `Tp_floor` — порог сублимации.

### 3.3 Свойства (фактически ab initio)
- `Pice` — Murphy & Koop (2005); `ΔHsub=2.84·10⁶ Дж/кг`; `k_ice=2.30 Вт/мК`.
- `Kv` — Tang & Pikal ур.6 (полу-эмпирика; газовая часть — кинетика газов).

### 3.4 Теоретический `Rp` без эксперимента (Кнудсен)
При `Pc~10 Па` длина свободного пробега (~1 мм) ≫ пор (~10–50 мкм) → режим Кнудсена:
```
Rp(H) = Rp_skin + (R·T)/(M·D_eff)·H,  D_eff = (ε/τ)·(2/3)·r_pore·v̄,  v̄=√(8RT/πM)
ε = 1 − φ_solid(концентрация),  τ = ε^(−1/2) (Бруггеман)
```
Проверка: 5 % сухого, `r_pore=18 мкм` → `Rp(1см)≈4.4` Torr·см²·ч/г (литература ~5).
Точность `Rp` ≈ ±50 %, почти вся ошибка — в `r_pore` (то есть в заморозке).

### 3.5 Оценка `Tg'`/`Tc` без эксперимента (`predict_tg.py`)
- Табличные `Tg'` чистых эксципиентов (надёжно).
- Gordon–Taylor (бинар + Cg') — грубо: сахароза −2.8 °C, но трегалоза/сорбитол ±11–14 °C.
- Смешение по Фоксу из табличных `Tg'` (надёжно, ±2–4 °C).
- `Tc ≈ Tg' + 2 °C` (аморфные) или `= Te` (кристаллические).

---

## 4. Главные физические выводы (что поняли в процессе)

### 4.1 Коллапс — это вязкое течение, а не просто «Tp>Tc»
Высохший freeze-concentrate выше `Tc` течёт (вязкость по WLF падает) и закрывает
поры быстрее ухода фронта. `Co=η(Tc)/η(Tp)`: `Co≥1 ⟺ Tp≥Tc`. Механистическое
обоснование правила Tang & Pikal, привязанное к измеряемой `Tc`.

### 4.2 Заморозка программирует Rp на всю сушку
Цепочка: переохлаждение → число зародышей → размер кристаллов → пористость/поры →
`Rp` → время сушки. Контролируемая нуклеация (крупные кристаллы, низкое `Rp`) и
быстрее, и безопаснее. Расчёт: ~24–35 % экономии времени.

### 4.3 Эффективный режим
**Контролируемая нуклеация (низкое `Rp`) + работа вплотную к `Tc` с запасом.**
Расчёт: до −60 % времени против перестраховки без коллапса.

### 4.4 Порог сублимации `Tice(Pc)` и точное соотношение на фронте
`Pice(Tp) = Pc + Js·Rp`. `Pice` ВСЕГДА > Pc (иначе `Js=0` — сушки нет). `Tp` сидит
**чуть выше** порога `Tice(Pc)` на величину, соответствующую `Js·Rp`. Порог —
асимптотическая нижняя граница, не рабочая точка. (Поправка к неаккуратному раннему
«Pice≈Pc»: поток ненулевой, избыток мал но положителен.)

### 4.5 Плоская vs растущая Tp — режим лимитирования (объяснение Ronzi Fig.3 a,b)
- **Теплолимит (низкое Rp):** `Js·Rp` мал → Tp прилипает к порогу, **~плоская и
  плате-независимая**; полка меняет лишь СКОРОСТЬ. Это и есть Ronzi.
- **Массолимит (высокое Rp):** `Js·Rp` велик и растёт → **Tp лезет вверх** и зависит
  от полки.
Наш ранний «монотонный рост» — артефакт ЗАВЫШЕННОГО `Rp` (мелкие поры). Физика
привязки к порогу в модели есть и включается при реалистичном (низком) `Rp`.

### 4.6 Что нельзя получить без эксперимента / коллигативность
- Переохлаждение `ΔTn` из одних `Tg'`+`Tim` НЕ извлекается (кинетика vs равновесие);
  даёт лишь верхнюю рамку `ΔTn ≤ Tf° − Tg'`. Реально — снять с термопары при заморозке
  (провал-отскок, бесплатно) или задать контролем.
- `Tf°` (начало замерзания) ≈ 0 − криоскопическое понижение, из концентрации
  (коллигативно), и это near-zero; ошибка коллигативки даже при 20 % налива ≤0.5 °C
  и пренебрежима против `ΔTn~15 °C`. Коллигативка применяется ТОЛЬКО к `Tf°`; к
  вымороженному концентрату (65–80 %, → `Tg'`) — Gordon–Taylor/таблицы.
- `Tf°` (начало, разбавленный, ≈ −1 °C) и `Tg'` (конец, ~80 % солидов, ≈ −32 °C) —
  ПРОТИВОПОЛОЖНЫЕ концы траектории вымораживания; `Tim` при нагреве близок к `Tg'`.

### 4.7 Провенанс формул (честно)
- `Rp(H)=Rp0+A1·H/(1+A2·H)` — из литературы (Pikal 1983/1985; Tang & Pikal 2004).
- `fcryst=(ΔTsc/ΔTref)^0.5` — МОЯ феноменология; физоснова: Searles 2001,
  Kasper & Friess 2011, Kurz & Fisher. Экспонента 0.5 не выведена строго; литература
  (Searles) скорее за ~1 (Rp ∝ ΔTn линейно). Коридор экспоненты 0.5–1.

---

## 5. Валидация против Ronzi 2003 (`ronzi_compare/`, отдельная папка)

Воспроизведён оптимизированный цикл Ronzi (Табл.4 / Fig.5), Factor VIII.
- Дефолт (Kv tubing, r=8 мкм, Tc=−32): t=15.8 ч, коллапс — НЕ совпало.
- **Калибровка** (Kv×2 — флакон 1 мл на алюминиевом подносе, r=40 мкм, Tc=−20 —
  их наблюдаемый коллапс): t=8.9 ч ≈ Ronzi ~9 ч, без коллапса. **Совпало.**

Выводы калибровки:
- `Kv` для их установки ≈ ×2 от Tang & Pikal (контакт с подносом + радиация камеры).
- При `Pc=20 Па` порог сублимации ≈ −31 °C → продукт не холоднее ~−25 °C при рампе
  до −5 °C → их эффективная `Tc≈−20 °C` (литературные −32 от Österberg, *другая*
  формуляция, слишком консервативны).

Также показано (`verify_shapes.py`, `regime_compare.py`): расхождение форм кривых с
корневым `results.png` — это разные величины (Tp фронта vs Tb дна) и режим полки
(постоянная → плато vs рампа → рост), а также режим лимитирования (см. 4.5).

---

## 6. Физоснованные усиления (deep-research → реализовано)

Реализованы 4 первопринципных уточнения (`enhancements.py`), без новых опытов:
- **E.** Извилистость `τ(ε)`: bruggeman/archie/**directional** (колонные поры → τ≈1).
  Важно лишь для плотных коржей (высокая концентрация).
- **D.** Dusty-gas: `D_eff=(ε/τ)D_K + B0·P̄/μ`, `B0=ε·r²/(8τ)`. Вязкая поправка
  растёт с давлением/порами: ~0 % при 0.05 Torr → ~8 % при 1 Torr. Критерий — Kn.
- **F.** Размер пор из ТЕОРИИ ЗАТВЕРДЕВАНИЯ: `v,G` из задачи Стефана при заморозке,
  `r_pore=r_ref·(v_ref/v)^0.5`. Непрерывно, заменяет таблицу `PORE_BY_REGIME`.
- **B.** Верхний путь тепла: радиация на сухую поверхность + теплопроводность сухого
  слоя (`k_eff` по Maxwell–Eucken). `solve_step` переписан на 2-путевой энергобаланс
  (обратно совместимо при `T_rad=None`).

**Ещё не сделано (следующий заход):** C (edge-vials/неоднородность партии, view
factors, 3-D) и J (камера–конденсатор: проводимость канала, choked flow,
самосогласованное `Pc(t)`). Это шаг к модели партии/установки.

---

## 7. Файлы проекта

| Файл | Назначение |
|---|---|
| `lyo_model.py` | модель: свойства, базовая Tang-Pikal, улучшенная динамическая, Кнудсен/dusty-gas Rp, заморозка→поры, коллапс, верхний путь тепла |
| `predict_tg.py` | оценка `Tg'`/`Tc` без эксперимента (Gordon-Taylor/Фокс) |
| `demo.py` → `results.png` | 3 эксперимента (заморозка, коллапс, оптимум) |
| `compare.py` → `comparison.png` | базовая vs улучшенная модель |
| `rp_demo.py` → `rp_theory.png` | теоретический `Rp` (Кнудсен) |
| `enhancements.py` → `enhancements.png` | усиления E/D/F/B |
| `ronzi_compare/` | валидация против Ronzi 2003 (compare/verify_shapes/regime_compare) |
| `README.md`, `REPORT.md` | документация и разбор |
| `KNOWLEDGE.md` | этот документ |

Запуск: `uv run python <script>.py`.

---

## 8. Ключевые источники

- Tang & Pikal, *Design of Freeze-Drying Processes for Pharmaceuticals: Practical
  Advice*, Pharm. Res. 21(2):191–200 (2004).
- Ronzi et al., *Optimisation of a freeze-drying process… Factor VIII and IX*,
  Chem. Eng. Process. 42:751–757 (2003).
- Kumar et al., *Application of lyophilization in pharmaceutical injectable
  formulations*, J. Drug Deliv. Sci. Technol. 100:106089 (2024).
- Pikal, Shah, Senior, Lang, J. Pharm. Sci. 72:635–650 (1983); Pikal, J. Parenter.
  Sci. Technol. 39:115–139 (1985) — `Rp(l)` и коэффициенты тепло/массопереноса.
- Searles, Carpenter, Randolph, J. Pharm. Sci. 90:860–871 и 872–887 (2001) —
  температура нуклеации определяет скорость сушки; SSA ∝ переохлаждение.
- Kasper & Friess, Eur. J. Pharm. Biopharm. 78:248–263 (2011) — обзор заморозки.
- Murphy & Koop, Q. J. R. Meteorol. Soc. 131:1539–1565 (2005) — `Pice(T)`.
- Mason & Malinauskas — dusty-gas model. Kurz & Fisher — теория затвердевания.
- Frenkel (1945) — вязкое спекание. Passot et al. (2017) — edge-vial radiation.
```
